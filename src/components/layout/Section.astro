---
interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<section
  id={id}
  class={`relative w-full py-20 md:py-32 border-b border-border/20 ${className}`}
>
  <slot />
</section>
