---
interface Props {
  as?: "p" | "div" | "span";
  class?: string;
}

const { as: Tag = "p", class: className } = Astro.props;
---

<Tag
  class={`font-sans text-base leading-relaxed text-secondary-foreground/80 ${className}`}
>
  <slot />
</Tag>
