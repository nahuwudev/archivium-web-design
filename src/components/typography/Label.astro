---
interface Props {
  as?: "span" | "p" | "div";
  class?: string;
}

const { as: Tag = "span", class: className } = Astro.props;
---

<Tag
  class={`font-mono text-xs uppercase tracking-wider text-muted-foreground ${className}`}
>
  <slot />
</Tag>
