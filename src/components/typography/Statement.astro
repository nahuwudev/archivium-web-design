---
interface Props {
  as?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6" | "p";
  class?: string;
}

const { as: Tag = "h2", class: className } = Astro.props;
---

<Tag
  class={`font-serif tracking-tight text-primary leading-[1.1] ${className}`}
>
  <slot />
</Tag>
